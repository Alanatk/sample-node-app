version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies

  build:
    commands:
      - echo Build started
      - npm install
      - npm run build

artifacts:
  files:
    - '**/*'
